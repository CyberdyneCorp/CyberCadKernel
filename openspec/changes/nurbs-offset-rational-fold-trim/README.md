# nurbs-offset-rational-fold-trim

Rational NURBS surface offset (homogeneous-form fit preserving weights) plus self-intersection fold TRIMMING: when the offset folds, trim the domain to the maximal fold-free region and return a valid offset over it instead of declining outright.
