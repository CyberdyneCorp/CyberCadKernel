# NURBS scope decision — general NURBS B-rep vs the bounded hybrid

**Status:** decision on the record (2026-07-10). **Verdict: do NOT build the general NURBS
B-rep core speculatively. The bounded analytic + freeform-hybrid approach is the correct scope
for the app as it exists; expand into NURBS only when a concrete freeform-surfacing feature on
the app roadmap forces it — and then incrementally, demand-driven.**

This document exists because "OCCT has NURBS and we don't" recurs as a question. The honest
framing below separates the *facts* (what OCCT/Parasolid provide, what the reference apps use)
from the *decision* (what this kernel should invest in, and when).

---

## 1. Do the reference apps have general NURBS? — Yes, foundationally

Every pro CAD tool the app is benchmarked against runs on a **commercial general NURBS B-rep
kernel**:

| App | Geometry kernel | NURBS B-rep? |
|---|---|---|
| **SolidWorks** | Siemens **Parasolid** | ✅ full — solids + the entire surfacing suite are NURBS |
| **Shapr3D** | Siemens **Parasolid** (licensed) | ✅ full — solids *and* surfaces are Parasolid NURBS B-rep |
| Onshape | Parasolid | ✅ |
| Fusion 360 | Autodesk **ASM** (Parasolid lineage) | ✅ |

So if the bar is *literal* parity, general NURBS is table stakes — it is the underlying
representation that makes **arbitrary combinations of operations robust** and enables **freeform
surfacing**. CyberCadKernel deliberately chose a different point in the design space (below).

---

## 2. What "NURBS in OCCT/Parasolid" actually is (a layered stack)

NURBS is not one feature — it is a stack. Here is the stack vs. what this kernel has today:

| Layer | OCCT / Parasolid | CyberCadKernel today |
|---|---|---|
| **Geometry** — rational B-spline curves/surfaces, arbitrary degree, knots/mults; De Boor eval + derivatives | full | ✅ **partial** — NURBS eval + projection (`src/native/numerics`), Bézier/BSpline trimmed faces (`shape.h`), STEP admission of B-spline surfaces/curves + trims |
| **Geometry algorithms** — knot insert/remove, degree elevate/reduce, split, reparametrize | full | ✅ **exact-NURBS kernel** — OCCT-free, substrate-free `src/native/math/bspline_ops.{h,cpp}`: knot insert/refine/remove, degree elevate/reduce, split, Bézier decompose, reparam for curves + tensor surfaces, rational-aware (homogeneous lift). Exact-preserving (knot-removal / degree-reduction report a bounded error and decline honestly). Host-analytic gate `tests/native/test_native_nurbs_ops.cpp`. Layers 2–8 below remain demand-gated. |
| **Fitting / approximation** — points→B-spline curve/surface, interpolation | full | 🟡 **partial** — OCCT-free, numsci-gated `src/native/math/bspline_fit.{h,cpp}` (NURBS-SCOPE Layer 7): chord-length / centripetal parametrization, averaging + approx knots, **non-rational** global curve INTERPOLATION (A9.1, `lin_solve` collocation), least-squares curve APPROXIMATION (A9.4/9.6, endpoints pinned, `lstsq`), and tensor-product surface interp/approx. Also **RATIONAL interpolation with PRESCRIBED weights** (`interpolateRationalCurve` / `interpolateRationalSurface`): given data points Qₖ + a positive weight wₖ each, lift to the homogeneous point (wₖQₖ, wₖ) ∈ R⁴, run the SAME collocation solve on the 4-D net (the weight coordinate is interpolated exactly like x/y/z), and project back to (pole, weight) — so the rational curve/surface passes through every EUCLIDEAN datum exactly; a projected non-positive control weight is a documented guard (declined, never divide by ≤ 0). Airtight host gate `tests/native/test_native_nurbs_fit.cpp` (non-rational interpolation exact ~1e-15, idempotent round-trip ~1e-14, monotone approximation convergence; **rational** Euclidean pass-through ~1e-9, a KNOWN rational unit CIRCLE / half-CYLINDER recovered at every node ~1e-15 and reconstructed POINTWISE by rational idempotence ~1e-15, recovered weights match the input, non-positive/mismatched-weight declines). **Weight ESTIMATION from unweighted data (Ma–Kruth)** — recovering the wₖ themselves — is the residual (the harder inverse problem, not attempted / never faked); advanced surfacing (skin/loft/Gordon) also residual. |
| **NURBS intersection (SSI)** — arbitrary NURBS↔NURBS incl. tangent/singular | full (`IntPatch`) | 🟡 **bounded** — analytic + bounded-freeform SSI; general skew-NURBS↔NURBS honestly declines |
| **NURBS B-rep boolean** — arbitrary trimmed-NURBS faces, tolerant, non-manifold | full (`BOPAlgo`) | 🟡 **bounded — first TWO exact-NURBS slices landed (L3-S1: NURBS face ∩ plane; L3-S2: NURBS face ∩ analytic CURVED face)** — **L3-S1** `src/native/boolean/nurbs_plane_split.h` (`nurbsFacePlaneSplit`) cuts a **genuine NURBS `FaceSurface`** (`Kind::BSpline`, non-rational first / rational admitted) by a planar half-space: NURBS-adapter `wall ∩ P` trace → WLine-`(u,v)`-read pcurve gated by `S(pcurve)==C` → `splitFaceSmoothTrim` → half-space keep → M0w **curved↔FLAT** cap weld → watertight+volume self-verify (host `tests/native/test_native_nurbs_plane_split.cpp` — CUT π·ρ²·c/2 + COMMON, partition closure, DISAGREED=0 + sim vs OCCT `BRepAlgoAPI_Cut`). **L3-S2** `src/native/boolean/nurbs_curved_split.h` (`nurbsFaceCurvedSplit`) extends that to an **analytic CURVED cutter** (Cylinder/Sphere/Cone): the seam is a curve on BOTH curved surfaces and the sew is **curved-NURBS↔analytic-CURVED** — NURBS-adapter ∩ curved-cutter-adapter trace → WLine-`(u,v)`-read fidelity on BOTH F and G → `splitFaceSmoothTrim` → **CURVED-solid membership** keep (`classifyPoint`) → **curved-G cap fan** (deflection-bounded planar-triangle fan on the true cutter surface, outer ring = exact seam nodes → watertight weld) → watertight+volume self-verify (host `tests/native/test_native_nurbs_curved_split.cpp` — a `Kind::BSpline` bowl cut by a SPHERE, the CUT keep side converging to the closed-form LENS `2π[zc·ρ²/2 − a·ρ⁴/4] − (2π/3)[Rs³−(Rs²−ρ²)^{3/2}]` monotonely as the mesh refines, χ=2, DISAGREED=0 + sim vs OCCT `BRepAlgoAPI_Cut/Common`). Both slices stay OCCT-free, add no `cc_*` ABI. The M2 spine covers the app's Bézier-wall poses; general NURBS↔NURBS where the cutter is itself **freeform** (both operands arbitrary NURBS — the freeform↔freeform sew), closed-loop-seeding-missed seams, and multi-crossing splits are the deep tail (see `openspec/L3-EXACT-NURBS-BOOLEAN-READINESS.md`). |
| **NURBS fillet/chamfer** — variable-radius / G2 on arbitrary surfaces | full (`ChFi3d`) | 🟡 **analytic-revolve + canal + G2** — OCCT-free constant/variable-radius rolling-ball fillets on planar dihedrals and analytic-revolve rims (cyl/cone/sphere↔cap, cyl↔cyl canal), plus **G2 (curvature-continuous)** blends: a **quintic** cross-section with prescribed end curvature via the pole rule `q=(5/4)·κ·h²`. **Planar** dihedrals (convex + concave) use the κ=0 collinear-triple special case (`src/native/blend/fillet_edges_g2{,_concave}.h`); the first **CURVED substrate** — a **sphere↔cap** rim — uses genuine curvature-MATCHING: the revolved meridian section curvature MATCHES the sphere's normal curvature `1/R` at the wall seam (≠ 0) and the cap's `0` at the cap seam (`src/native/blend/curved_fillet_g2.h`), welded watertight, volume convergent under refinement. Host gates in `tests/native/test_native_blend.cpp` prove closed-form + discrete-measured curvature continuity with the G1 circular-section `1/r` jump as a non-trivial control. General variable-radius / arbitrary-freeform G2 still declines → OCCT. |
| **NURBS offset / thicken** | full (`BRepOffset`) | 🟡 surface offset + solid thicken/shell (partial) |
| **NURBS fillet/chamfer** — variable-radius / G2 on arbitrary surfaces | full (`ChFi3d`) | 🟡 **analytic-revolve + canal + G2** — OCCT-free constant/variable-radius rolling-ball fillets on planar dihedrals and analytic-revolve rims (cyl/cone/sphere↔cap, cyl↔cyl canal), plus **G2 (curvature-continuous)** blends: a **quintic** cross-section with prescribed end curvature via the pole rule `q=(5/4)·κ·h²`. **Planar** dihedrals (convex + concave) use the κ=0 collinear-triple special case (`src/native/blend/fillet_edges_g2{,_concave}.h`); the first **CURVED substrate** — a **sphere↔cap** rim — uses genuine curvature-MATCHING: the revolved meridian section curvature MATCHES the sphere's normal curvature `1/R` at the wall seam (≠ 0) and the cap's `0` at the cap seam (`src/native/blend/curved_fillet_g2.h`), welded watertight, volume convergent under refinement. The next curved slice — **cylinder↔cap and cone↔cap** rims (`curved_fillet_edge_g2_cyl` / `_cone`, same header) — is **direction-aware**: a cylinder/cone wall is STRAIGHT-RULED, so its normal curvature IN THE SECTION (meridian) PLANE at the seam is `0` (the ruling; "0 along the axis") while its HOOP curvature `1/Rc` (cone: `cosσ/ρ`) is matched AUTOMATICALLY by the revolution once G1 fixes the seam normal radial — so the genuine G2 match is `κ_meridian=0` at BOTH seams (the `q=0` collinear triple, only the cone's wall tangent tilts by σ). This is NOT the planar case: the substrate is curved (nonzero hoop) and the blend's hoop-curvature continuity is a real second-order match; the G1 torus tube's `1/r` meridian curvature is the JUMP the quintic removes. Host gates prove closed-form + discrete-measured `κ_meridian→0` and the `1/Rc` hoop match with the G1 `1/r` jump as a non-trivial control, watertight + refinement-convergent volume, and honest declines with no cross-firing among the sphere/cyl/cone builders. **VARIABLE-radius G2 on a straight planar dihedral** (`src/native/blend/fillet_edges_g2_variable.h`, `fillet_edges_g2_variable`): the section radius ramps linearly `r(τ)=r0(1−τ)+r1·τ` along the (straight) edge, each cross-section the constant-`r` zero-end-curvature quintic for its LOCAL radius, LOFTED along the edge — every section lives in the cross-plane ⊥ the crease, so the crease coordinate is strictly injective ⇒ the swept surface is EMBEDDED (no cross-station self-intersection) and G2 holds `κ=0` at BOTH rails at EVERY station (not just the two ends), while the four tangent-line endpoints stay coplanar so a SINGLE diagonal wedge clip sets both faces back (the `r0==r1` case is exactly the constant-radius G2 fillet). Self-intersection guard: a too-fast ramp `|r1−r0|≥L` (setback line recedes faster than the edge advances → trimmed face folds) DECLINES, never returned self-intersecting. Closed-form monotone removed volume `(131/960)·L·(r0²+r0r1+r1²)/3`; host gates prove B''=0 at both rails over ALL stations + a discrete Menger witness far below the G1 `1/r(τ)` jump, watertight + volume match, `r0==r1` reduction, reversed-law symmetry, the fast-ramp decline, and concave/curved/degenerate declines with no cross-firing. General variable-radius on CURVED substrates, concave-variable, non-linear laws, and arbitrary-freeform G2 still decline → OCCT. |
| **NURBS offset / thicken** | full (`BRepOffset`) | 🟡 analytic walls only |
| **Exact NURBS sweep/loft/surfacing** — GeomFill/BRepFill, Gordon/network/plate | full | 🟡 **partial** — OCCT-free, numsci-gated (NURBS-SCOPE Layer 6). **Skinning / lofting** (`src/native/math/bspline_skin.{h,cpp}`): **non-rational** exact skinning (*The NURBS Book* §10.3 / A10.3) — section compatibility (raise to common degree + merge to union knots via the Layer-1 exact `elevateDegreeCurve`/`refineKnotCurve`, no geometry drift) then a tensor-product surface interpolated across the sections in V (Layer-7 `lin_solve` collocation); host gate `tests/native/test_native_nurbs_skin.cpp` — surface **contains every section pointwise** (~1e-15), compatible sections equal originals (~1e-15), known-surface round-trip + idempotent identity (~1e-15). **RATIONAL skinning** now landed too (`skinRationalSurface` / `makeRationalSectionsCompatible`): weighted sections are made compatible with the rational-aware Layer-1 elevate/refine (homogeneous — weights preserved exactly) then interpolated across the sections in HOMOGENEOUS R⁴ (the SAME collocation matrix solved for all four `(w·P, w)` coordinates) and projected back to a rational surface — the rational surface **contains every rational section pointwise** (~1e-9, achieved ~1e-15), a stack of rational circles gives iso-curves that are TRUE circles (radius exact), and rational skinning of different-radius circles is a rational cone/frustum; a projected non-positive control weight declines honestly. **Swept surfaces** (`src/native/math/bspline_sweep.{h,cpp}`, §10.4): **non-rational** — a *translational* sweep is the EXACT tensor product of the section with a degree-1 path (every iso is the section translated, machine-exact, no fitting), and a *general* sweep places the section at K stations along a trajectory using a **rotation-minimizing moving frame** (double-reflection, Wang et al. 2008 — no torsion-driven twist) then SKINS them; host gate `tests/native/test_native_nurbs_sweep.cpp` — translational exactness (~1e-15), station containment (~1e-15), closed-form ruled/cylinder-like patches, anti-twist frame sanity. **RATIONAL sweeps** also landed: `sweepRationalTranslational` is the EXACT rational tensor product of a rational section × degree-1 path (weights constant in V), so a rational quadratic **CIRCLE** swept translationally is an EXACT rational **CYLINDER** matching the analytic cylinder POINTWISE (~1e-9, achieved ~1e-15 — the strongest oracle, proving an exact rational surface not a facet), and `sweepRationalAlongTrajectory` places the rational section by rigid frames (weights preserved exactly) then rational-skins (station containment ~1e-8); non-positive/mismatched-weight + rational-trajectory declines. **Gordon / network surface** (`src/native/math/bspline_gordon.{h,cpp}`, §10.5): **non-rational** — a surface through a NETWORK of `K` u-curves and `L` v-curves intersecting at a `K×L` grid, built as the **boolean sum** `G = S_u ⊕ S_v ⊖ T` (skin of the u-curves + skin of the v-curves − tensor-product interpolant of the grid, brought to a common basis via the Layer-1 exact `elevateDegreeSurface`/`refineKnotSurface` then nets added/subtracted); declines honestly on an inconsistent/degenerate network (curve intersections must match the grid within tol). Host gate `tests/native/test_native_nurbs_gordon.cpp` — surface **contains every u- and v-curve pointwise** (~5e-15), grid intersections on-surface (~1e-15), known-surface round-trip (~1e-6) + idempotent full-surface identity (~4e-15), honest inconsistent/rational/degenerate declines. **Rational skinning + rational swept surfaces landed** (change `nurbs-rational-surfacing`); rational **Gordon/network**, **irregular / N-sided** networks (non-grid topologies, boundary/plate filling), **rotational** (revolved) sweeps, exact **GeomFill/BRepFill** continuous sweeps (the ~1e-6 averaging-knot reconstruction residual), and **variable-section** sweeps are the residual — general lofts/sweeps still fall back to *faceted* results. |
| **Exact NURBS sweep/loft/surfacing** — GeomFill/BRepFill, Gordon/network/plate | full | 🟡 **partial** — OCCT-free, numsci-gated (NURBS-SCOPE Layer 6). **Skinning / lofting** (`src/native/math/bspline_skin.{h,cpp}`): **non-rational** exact skinning (*The NURBS Book* §10.3 / A10.3) — section compatibility (raise to common degree + merge to union knots via the Layer-1 exact `elevateDegreeCurve`/`refineKnotCurve`, no geometry drift) then a tensor-product surface interpolated across the sections in V (Layer-7 `lin_solve` collocation); host gate `tests/native/test_native_nurbs_skin.cpp` — surface **contains every section pointwise** (~1e-15), compatible sections equal originals (~1e-15), known-surface round-trip + idempotent identity (~1e-15). **Swept surfaces** (`src/native/math/bspline_sweep.{h,cpp}`, §10.4): **non-rational** — a *translational* sweep is the EXACT tensor product of the section with a degree-1 path (every iso is the section translated, machine-exact, no fitting), and a *general* sweep places the section at K stations along a trajectory using a **rotation-minimizing moving frame** (double-reflection, Wang et al. 2008 — no torsion-driven twist) then SKINS them; host gate `tests/native/test_native_nurbs_sweep.cpp` — translational exactness (~1e-15), station containment (~1e-15), closed-form ruled/cylinder-like patches, anti-twist frame sanity. **Gordon / network surface** (`src/native/math/bspline_gordon.{h,cpp}`, §10.5): **non-rational** — a surface through a NETWORK of `K` u-curves and `L` v-curves intersecting at a `K×L` grid, built as the **boolean sum** `G = S_u ⊕ S_v ⊖ T` (skin of the u-curves + skin of the v-curves − tensor-product interpolant of the grid, brought to a common basis via the Layer-1 exact `elevateDegreeSurface`/`refineKnotSurface` then nets added/subtracted); declines honestly on an inconsistent/degenerate network (curve intersections must match the grid within tol). Host gate `tests/native/test_native_nurbs_gordon.cpp` — surface **contains every u- and v-curve pointwise** (~5e-15), grid intersections on-surface (~1e-15), known-surface round-trip (~1e-6) + idempotent full-surface identity (~4e-15), honest inconsistent/rational/degenerate declines. **Boundary-filled Coons patch** (`src/native/math/bspline_coons.{h,cpp}`, §10.5 / Coons 1967): **non-rational** — fill a CLOSED four-sided boundary (curves `c0(u)` at v=0, `c1(u)` at v=1, `d0(v)` at u=0, `d1(v)` at u=1, corners shared) with a tensor-product surface INTERPOLATING all four boundaries, built as the bilinearly-blended **boolean sum** `Coons = L_u ⊕ L_v ⊖ B` (ruled surface between d0,d1 + ruled surface between c0,c1 − the degree-(1,1) bilinear tensor of the four corners, brought to a common basis via the Layer-1 exact `elevateDegreeCurve`/`refineKnotCurve` + `elevateDegreeSurface`/`refineKnotSurface` then nets added/subtracted — no linear solve); the core surfacing op for **filling holes / capping openings / lofting a boundary**; declines honestly on a mismatched-corner / rational / degenerate boundary. Host gate `tests/native/test_native_nurbs_coons.cpp` — surface **contains all four boundary curves pointwise** (~1.8e-15), corner interpolation exact (0), flat boundary → flat patch matching the plane (0), known-surface round-trip (a ruled/bilinear surface recovered POINTWISE ~1.8e-15 — Coons is exact for bilinearly-blended surfaces; a general surface's four boundaries contained ~9e-16), honest mismatched-corner/rational/malformed declines. Rational skinning/sweep/Gordon/Coons, **irregular / N-sided** fill (5+ boundaries, degenerate-corner / triangular patches; non-grid networks), **Gregory / energy-minimizing PLATE** blends (G1/G2 tangent/curvature continuity to adjacent surfaces — Coons matches boundary POSITION only), **rotational** (revolved) sweeps, exact **GeomFill/BRepFill** continuous sweeps (the ~1e-6 averaging-knot reconstruction residual), and **variable-section** sweeps are the residual — general lofts/sweeps still fall back to *faceted* results. |
| **NURBS fillet/chamfer** — variable-radius / G2 on arbitrary surfaces | full (`ChFi3d`) | 🟡 analytic-revolve + canal only |
| **NURBS offset / thicken** | full (`BRepOffset`) | 🟡 **partial** — OCCT-free, numsci-gated `src/native/math/bspline_offset.{h,cpp}` (NURBS-SCOPE Layer 5): **surface OFFSET** by normal-locus fit — sample the true offset locus `S + d·N` on an adaptive grid and fit a **non-rational** NURBS surface through it (Layer-7 `interpolateSurface`), refining until the fit's deviation from the locus is within tol; the offset error is the ACHIEVED deviation (never widened). Honest guards: a self-intersecting offset (`|d|` past a principal radius of curvature → fold, detected from the 2nd fundamental form) and a degenerate/near-null-normal patch are DECLINED with a reason, never returned folded. Airtight host gate `tests/native/test_native_nurbs_offset.cpp`: every offset point at distance `|d|` from `S` (dense projection), analytic cylinder (radius `r±d`) + exact plane (~1e-9), monotone error convergence, self-intersection guard (radius ≈ curvature radius). **SOLID thicken/shell** now landed on top of the offset (`src/native/math/bspline_thicken.{h,cpp}`, Layer 5): an open surface `S` + its offset panel `S+d·N` + four ruled side walls sewn — with **shared exact boundary vertices** — into a CLOSED, watertight triangle shell (`tessellate::Mesh`), coherently oriented, closure PROVEN (χ=2, zero boundary edges) not asserted; the offset layer's fold guard is inherited (a thicken past the curvature radius DECLINES, never returns a folded/self-intersecting solid). Airtight host gate `tests/native/test_native_nurbs_thicken.cpp`: watertight (χ=2) for flat/curved/rational patches, enclosed volume == `area·|d|` exactly for a flat box (≈ for a thin slab / annular-wedge closed form for a cylinder shell), offset cap at distance `|d|` from `S`, and the fold/degenerate/zero-thickness declines. **MULTI-FACE shell** now landed on top (`src/native/math/bspline_shell.{h,cpp}`, Layer 5): a small B-rep of 2+ edge-adjacent NURBS faces thickened into ONE closed watertight solid — offset EACH face, and along a SHARED interior edge the two inner faces **meet directly** (welded at the shared model edge, **NO interior double-wall**) while full side walls are built only on the OUTER boundary; the offset side welds directly for coplanar/tangent faces and is joined by a **MITRE** (offset planes extended to their meeting corner) at a dihedral corner, with 3+-face corners welded to one vertex. Closure PROVEN (χ=2, zero boundary edges, coherent orientation), the offset fold guard inherited (any face past its curvature radius DECLINES the whole shell). Airtight host gate `tests/native/test_native_nurbs_shell.cpp`: a 2-patch coplanar-pair / L-shape / curved-pair thickens to ONE closed solid, two coplanar rectangles sharing an edge == EXACTLY one box (volume == `total_area·|d|` == `Σ per-face area·|d|`, shared edge not double-counted, ~1e-9), the shared interior edge carries NO wall (every offset-side seam edge used twice), and fold/degenerate/inconsistent-adjacency/zero-thickness declines. **Robust self-intersecting-shell trimming** (recover rather than decline), **exact union volume for interpenetrating dihedral slabs** (needs face trimming at face–face intersections; the coplanar box is exact, a right-angle L-shape is watertight only), and rational offset fit are the residual — analytic walls still cover the app's common cases. |
| **Exact NURBS sweep/loft/surfacing** — GeomFill/BRepFill, Gordon/network/plate | full | 🟡 **partial** — OCCT-free, numsci-gated `src/native/math/bspline_skin.{h,cpp}` (NURBS-SCOPE Layer 6): **non-rational** exact **skinning / lofting** (*The NURBS Book* §10.3 / A10.3) — section compatibility (raise to common degree + merge to union knots via the Layer-1 exact `elevateDegreeCurve`/`refineKnotCurve`, no geometry drift) then a tensor-product surface interpolated across the sections in V (Layer-7 `lin_solve` collocation). Airtight host gate `tests/native/test_native_nurbs_skin.cpp`: the surface **contains every section pointwise** (~1e-15), compatible sections equal their originals (~1e-15), known-surface iso-curve round-trip + idempotent full-surface identity (~1e-15). Rational skinning, general Gordon/network/boundary surfacing, and exact swept surfaces (GeomFill/BRepFill) are the residual — general lofts still fall back to *faceted* results. |
| **NURBS offset / thicken** | full (`BRepOffset`) | 🟡 **partial** — OCCT-free, numsci-gated `src/native/math/bspline_offset.{h,cpp}` (NURBS-SCOPE Layer 5): **surface OFFSET** by normal-locus fit — sample the true offset locus `S + d·N` on an adaptive grid and fit a **non-rational** NURBS surface through it (Layer-7 `interpolateSurface`), refining until the fit's deviation from the locus is within tol; the offset error is the ACHIEVED deviation (never widened). Honest guards: a self-intersecting offset (`|d|` past a principal radius of curvature → fold, detected from the 2nd fundamental form) and a degenerate/near-null-normal patch are DECLINED with a reason, never returned folded. Airtight host gate `tests/native/test_native_nurbs_offset.cpp`: every offset point at distance `|d|` from `S` (dense projection), analytic cylinder (radius `r±d`) + exact plane (~1e-9), monotone error convergence, self-intersection guard (radius ≈ curvature radius). Solid **thicken/shell/hollow** (offset both faces + stitch side walls into a closed solid), robust self-intersecting-offset **trimming**, and rational offset fit are the residual — analytic walls still cover the app's common cases. |
| **Exact NURBS sweep/loft/surfacing** — GeomFill/BRepFill, Gordon/network/plate | full | 🟡 **partial** — OCCT-free, numsci-gated `src/native/math/bspline_skin.{h,cpp}` (NURBS-SCOPE Layer 6): **non-rational** exact **skinning / lofting** (*The NURBS Book* §10.3 / A10.3) — section compatibility (raise to common degree + merge to union knots via the Layer-1 exact `elevateDegreeCurve`/`refineKnotCurve`, no geometry drift) then a tensor-product surface interpolated across the sections in V (Layer-7 `lin_solve` collocation). Airtight host gate `tests/native/test_native_nurbs_skin.cpp`: the surface **contains every section pointwise** (~1e-15), compatible sections equal their originals (~1e-15), known-surface iso-curve round-trip + idempotent full-surface identity (~1e-15). **Gordon / network surfaces** (`src/native/math/bspline_gordon.{h,cpp}`, §10.5) also landed: a surface through a NETWORK of u- and v-curves as the boolean sum `G = S_u ⊕ S_v ⊖ T`, interpolating every network curve pointwise (host gate `tests/native/test_native_nurbs_gordon.cpp`), declining honestly on an inconsistent network. Rational skinning/Gordon, irregular / N-sided networks + boundary/plate surfacing, and exact swept surfaces (GeomFill/BRepFill) are the residual — general lofts still fall back to *faceted* results. |
| **Trimmed-NURBS B-rep data model** — exact pcurves, tolerant topology, NURBS healing | full | 🟡 **partial** — OCCT-free `src/native/topology/trimmed_nurbs.{h,cpp}` (NURBS-SCOPE Layer 8): a `TrimmedNurbsFace` = surface + outer/hole trim loops of `(u,v)` pcurve segments (reusing the `shape.h` `FaceSurface`/`PCurve` storage, no shape.h change); robust **point-in-trimmed-region** classification (`In`/`Out`/`OnBoundary`/`Unknown`, honest declines for degenerate/self-touching loops); a **pcurve-fidelity** guard verifying `S(pcurve(t)) == C(t)` on a dense sample within a scale-relative tol (a wrong pcurve is DETECTED, ~1e-9 on an exact iso-curve); and numsci-gated **pcurve construction** (project edge samples via `closest_point_on_surface` + fit a 2-D B-spline via `bspline_fit`, round-trip-verified). Airtight host gate `tests/native/test_native_trimmed_nurbs.cpp`. General **tolerant-topology healing** (auto-closing gapped loops, resolving pinch points, snapping near-coincident pcurves) + rational pcurve fitting + the exact-B-rep boolean this feeds (Layer 3) are the residual. |
| **Trimmed-NURBS B-rep data model** — exact pcurves, tolerant topology, NURBS healing | full | 🟡 **partial** — OCCT-free `src/native/topology/trimmed_nurbs.{h,cpp}` (NURBS-SCOPE Layer 8): a `TrimmedNurbsFace` = surface + outer/hole trim loops of `(u,v)` pcurve segments (reusing the `shape.h` `FaceSurface`/`PCurve` storage, no shape.h change); robust **point-in-trimmed-region** classification (`In`/`Out`/`OnBoundary`/`Unknown`, honest declines for degenerate/self-touching loops); a **pcurve-fidelity** guard verifying `S(pcurve(t)) == C(t)` on a dense sample within a scale-relative tol (a wrong pcurve is DETECTED, ~1e-9 on an exact iso-curve); and numsci-gated **pcurve construction** (project edge samples via `closest_point_on_surface` + fit a 2-D B-spline via `bspline_fit`, round-trip-verified). Bounded, **region-preserving tolerant-topology HEALING** (`healLoop`/`healTrimLoop`, run by `classify`): a small inter-segment **gap** or a **near-coincident pcurve** pair within a scale-relative tol is **welded** closed (each vertex moves ≤ tol/2, so a heal NEVER flips an interior/exterior point's In/Out verdict — proven by a gap-size sweep in the gate); a genuine **large gap** (> tol) still DECLINES honestly (`Unknown`, never force-welded); a self-touching **pinch** is DETECTED, and a **clean 2-way pinch** (a figure-eight self-touching at one vertex) is **SPLIT** into two independent sub-loops (`splitAtPinch`/`splitTrimLoopAtPinch`, opt-in via `classify`'s `splitPinch`) whose union classifies every point IDENTICALLY to the original pinched loop — region-preserving (proven against a two-loop reference in the gate); a **3+-way / crossing pinch** is `ambiguous` and declined honestly. Airtight host gate `tests/native/test_native_trimmed_nurbs.cpp` (small-gap-heals-identical, large-gap-declines, containment-preservation sweep, near-coincident-snap, pinch-detected, **figure-eight-splits-identically**, un-splittable-pinch-declines, clean-loop-unaffected). **General non-manifold repair** (3+-way/crossing-pinch resolution, seam-crossing healing, re-parametrizing a badly-drifting pcurve) + rational pcurve fitting (the **rational residual**) + the exact-B-rep boolean this feeds (Layer 3) are the residual. |

**Key architectural fact:** this kernel is an **analytic + tessellation hybrid with bounded
freeform support** — it *evaluates* NURBS and handles *bounded* freeform intersection/boolean/mesh,
but it is **not** a general exact-NURBS B-rep modeler. That was a deliberate choice: it let the
drop-OCCT campaign serve the app's real needs at ~15–30 py total instead of the ~20–40 py the
general NURBS core alone would cost (§4).

---

## 3. Is it useful for *this* app? — depends on the product direction

NURBS is mostly the *underlying representation*, not a user-facing feature. What users actually
*do* splits into two regimes:

| Regime | Needs general NURBS? | The app today |
|---|---|---|
| **Mechanical / parametric CAD** — sketch → extrude/revolve, booleans, fillet/chamfer, shell, draft, patterns, mates, on mostly analytic + simple-curved geometry | **No** — the bounded approach covers the common cases | ✅ this is exactly the measured `cc_*` usage (see DROP-OCCT-READINESS / APP-MIGRATION-READINESS) |
| **Freeform / industrial-design surfacing** — loft/boundary surfaces between arbitrary curves, class-A styling, organic / sub-D→NURBS, filling complex gaps, blends over already-curved regions | **Yes** — this is where the analytic/bounded approach runs out | ❌ not in the app's current surface |

**CAE angle specifically:** CAE (FEA/CFD prep) does **not** drive NURBS. It needs *good meshes* +
*accurate watertight geometry* — already provided by the native tessellator + TetGen volume
meshing + the render-quality display mesh. NURBS would matter for CAE only for isogeometric
analysis (niche) or high-fidelity NURBS *import* — and STEP already covers exact B-rep exchange.

---

## 4. Programming-years to build the general NURBS B-rep core

Rough, dependency-ordered (each builds on the one above):

| Sub-capability | Est. py | Note |
|---|---:|---|
| Complete exact-NURBS geometry kernel (knot ops, elevation, split, robust rational eval) | 1–2 | much of the eval exists |
| **General NURBS↔NURBS SSI** (all configs, tangent/singular robust) | 3–6 | research-grade; OCCT `IntPatch` is decades-refined |
| **General exact-NURBS B-rep boolean** (BOPAlgo-class, tolerant, non-manifold) | 5–10 | the hardest — *the core of a B-rep kernel* |
| General NURBS fillet/chamfer (ChFi3d-class) | 3–6 | needs the boolean + SSI |
| NURBS offset / thicken (robust) | 2–4 | |
| Exact NURBS sweep/loft/surfacing (GeomFill/Gordon/plate) | 2–4 | 🟡 non-rational **+ rational** exact **skinning/lofting** (`src/native/math/bspline_skin`, Layer 6) + **swept surfaces landed** (`src/native/math/bspline_sweep`, Layer 6): translational sweep = exact tensor product (iso == section translated), general sweep = section placed at K stations along a rotation-minimizing frame then skinned (surface contains every placed section pointwise); **rational skinning/sweep landed** (weighted sections lofted in homogeneous R⁴; a rational circle swept = an EXACT rational cylinder ~1e-15); rational Gordon/network + irregular/N-sided/boundary surfacing + rotational/exact-BRepFill/variable-section sweeps residual |
| NURBS offset / thicken (robust) | 2–4 | 🟡 non-rational **surface offset + single-patch + multi-face solid thicken/shell landed** (`src/native/math/bspline_offset` + `bspline_thicken` + `bspline_shell`, Layer 5): offset = normal-locus sample + NURBS fit + adaptive refine, with a 2nd-fundamental-form self-intersection (fold) guard and honest error reporting; single-patch thicken = offset panel + original panel + four ruled walls sewn (shared exact boundary vertices) into a CLOSED watertight shell (`tessellate::Mesh`), closure proven (χ=2) + volume == `area·|d|` for a box + inherited fold guard; **multi-face shell** = offset each of 2+ edge-adjacent faces + weld shared interior edges (NO double-wall) + mitre the offset side at dihedral corners + walls only on the outer boundary → ONE closed watertight solid (two coplanar rects sharing an edge == exact box, `total_area·|d|`); robust self-intersecting-shell trimming + exact union volume for interpenetrating dihedral slabs (face trimming) + rational offset residual |
| Exact NURBS sweep/loft/surfacing (GeomFill/Gordon/plate) | 2–4 | 🟡 non-rational exact **skinning/lofting landed** (`src/native/math/bspline_skin`, Layer 6): section compatibility + tensor-product interpolation across sections, surface contains every section pointwise; rational skinning + Gordon/network/boundary surfacing + exact swept surfaces residual |
| Exact NURBS sweep/loft/surfacing (GeomFill/Gordon/plate) | 2–4 | 🟡 non-rational exact **skinning/lofting** (`src/native/math/bspline_skin`, Layer 6) + **swept surfaces** (`src/native/math/bspline_sweep`, Layer 6) + **Gordon/network surfaces landed** (`src/native/math/bspline_gordon`, Layer 6): translational sweep = exact tensor product (iso == section translated), general sweep = section placed at K stations along a rotation-minimizing frame then skinned, Gordon = boolean sum `S_u ⊕ S_v ⊖ T` through a u/v curve network (contains every network curve pointwise, declines on inconsistent networks), **boundary-filled Coons patch landed** (`src/native/math/bspline_coons`, Layer 6): fill a closed four-sided boundary as the bilinear boolean sum `Coons = L_u ⊕ L_v ⊖ B` (interpolates all four boundary curves pointwise, exact for bilinearly-blended surfaces, declines on mismatched corners); rational skinning/sweep/Gordon/Coons + irregular/N-sided fill + Gregory/plate G1/G2 surfacing + rotational/exact-BRepFill/variable-section sweeps residual |
| NURBS offset / thicken (robust) | 2–4 | 🟡 non-rational **surface offset landed** (`src/native/math/bspline_offset`, Layer 5): normal-locus sample + NURBS fit + adaptive refine, with a 2nd-fundamental-form self-intersection (fold) guard and honest error reporting; solid thicken/shell/hollow + robust fold-trimming + rational offset residual |
| Exact NURBS sweep/loft/surfacing (GeomFill/Gordon/plate) | 2–4 | 🟡 non-rational exact **skinning/lofting + Gordon/network surfaces landed** (`src/native/math/bspline_skin` + `bspline_gordon`, Layer 6): section compatibility + tensor-product interpolation across sections; Gordon = boolean sum `S_u ⊕ S_v ⊖ T` through a u/v curve network, contains every network curve pointwise; rational skinning/Gordon + irregular/N-sided-network/boundary surfacing + exact swept surfaces residual |
| Fitting / approximation (points→NURBS) | 1–2 | 🟡 non-rational curve+surface interp/approx **landed** (`src/native/math/bspline_fit`, Layer 7); rational (weighted) fitting + advanced surfacing residual |
| Trimmed-NURBS B-rep data model + pcurve robustness + NURBS healing | 2–4 | 🟡 **partial** — trimmed-NURBS data model + point-in-region classification + pcurve fidelity/construction + **bounded region-preserving healing** (gap-closing, near-coincident-pcurve snapping, pinch DETECTION + **clean 2-way pinch-SPLITTING** into two region-preserving sub-loops; large-gap / 3+-way-or-crossing-pinch declined honestly) **landed** (`src/native/topology/trimmed_nurbs`, Layer 8); general non-manifold repair (3+-way/crossing-pinch resolution, seam-crossing healing) + rational pcurve fitting residual |
| Fitting / approximation (points→NURBS) | 1–2 | 🟡 non-rational curve+surface interp/approx + **rational interpolation with PRESCRIBED weights landed** (`src/native/math/bspline_fit`, Layer 7): homogeneous lift → same collocation solve → project; passes through Euclidean data exactly, recovers a KNOWN rational circle/cylinder pointwise (idempotence); **weight ESTIMATION (Ma–Kruth)** + advanced surfacing residual |
| Trimmed-NURBS B-rep data model + pcurve robustness + NURBS healing | 2–4 | 🟡 **partial** — trimmed-NURBS data model + point-in-region classification + pcurve fidelity/construction + **bounded region-preserving healing** (gap-closing, near-coincident-pcurve snapping, pinch DETECTION; large-gap/pinch declined honestly) **landed** (`src/native/topology/trimmed_nurbs`, Layer 8); general non-manifold repair (pinch-splitting into valid faces, seam-crossing healing) + rational pcurve fitting residual |
| **Total (OCCT/Parasolid-class general NURBS modeling)** | **≈ 20–40 py** | ~a decade for a small team |

For scale: this is **~10× the entire remaining drop-OCCT moat** (~1.5–3.5 py). Parasolid/OCCT
each represent ~25 years of this work; it is not obtained cheaply.

---

## 5. Decision

- **Do NOT take on the general NURBS core as one program.** It is the single largest possible
  scope item, and the app as it exists (mechanical/parametric per its measured `cc_*` usage) does
  not consume it.
- **The bounded pieces are the correct 80/20** — bounded N-sided fill (Coons/Gregory patch over
  analytic boundaries → tessellated fill), bounded freeform booleans, curved blends — deliver the
  practical freeform value at a tiny fraction of the cost, and are landed / landing demand-driven.
- **Trigger to reconsider:** if the app commits to genuine **freeform / organic / class-A
  surfacing** (the Shapr3D "arbitrary curves → smooth NURBS skin → tweak" workflow). Then build the
  NURBS layers **incrementally, driven by the specific surfacing features committed to** — SSI and
  boolean first, as they gate the rest — never speculatively.
- **CAE does not change this** — it is served by meshing + accurate geometry + STEP exchange, all
  already present.

**One-line summary:** the reference apps have general NURBS (via Parasolid) and it is foundational
to them, but it is a ~20–40 py investment this app does not need for mechanical/parametric CAD+CAE;
pursue it only if — and incrementally as — the product moves into freeform surfacing.
